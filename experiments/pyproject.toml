[project]
name = "pace-q1-experiments"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
analysis = ["jsonschema>=4.18", "matplotlib>=3.7", "pandas>=2.0", "pyarrow>=12.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
