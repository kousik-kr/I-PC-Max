# PACE Paper Requirements Matrix

Audit date: 2026-07-21

Paper: `Information_Systems___2026 (2).pdf`, 28-page draft supplied by the user.

## Baseline

| Item | Observed value |
|---|---|
| Commit | `19bb54b36cf3344244ad62819b9510c5df53f15b` |
| Branch | `main` |
| Worktree | Dirty before this audit. It contains prior query-generation work, dataset changes, and untracked generated data. No pre-existing change was reverted. |
| Java | Oracle JDK 21.0.11 |
| Maven | 3.9.16 |
| Baseline test | `mvn test`: 134 tests, 0 failures, 0 errors, 0 skipped |
| Current verification | `mvn test`: 152 tests, 0 failures, 0 errors, 0 skipped |

Status means evidence at this checkout, not intent inferred from a class or test name.

## Definitions 1-7

| Paper requirement | Expected behavior | Evidence found in repository | Status | Risk | Exact TODO prompt |
|---|---|---|---|---|---|
| Definition 1: time-dependent road network (paper p3) | A directed graph whose directed edges carry arrival and preference-score functions; parallel arcs remain distinct. | `TDGraph`, `Edge`, `GeneratedGraphLoader`; `TDGraphTest`, `GeneratedGraphLoaderTest`. Arc IDs are explicit and outgoing edges are stably ordered. | implemented | low | `Add a loader fixture with two parallel arcs and assert that both unique arc IDs and their independent functions survive loading.` |
| Definition 2: time-dependent edge functions (p3) | Continuous-time, continuous nondecreasing PWL arrival functions, derived travel time `A_e(t)-t`, FIFO, and PWC scores. | `PiecewiseLinearFn`, `PiecewiseConstFn`, `GraphValidator`; `PiecewiseFunctionTest`, `PiecewiseConstExactTest`. Temporal values are stored as canonicalized `double`, not exact integer/rational values. | partial | high | `Document the canonical numerical model and add a high-precision differential suite for PWL roots, flat preimages, endpoint ownership, and score boundaries; remove unqualified numerical-exactness claims if differences remain.` |
| Definition 3: path profiles (p3-4) | Propagate actual edge-entry times; calculate final arrival and travel time; evaluate each edge score at its actual entry time. | `CanonicalPathProfileBuilder`, `ExactPathProfileBuilder`, `TemporalStitch`; `PaceP0SynchronizationTest`, `TemporalStitchContinuousTest`, `IntervalForwardLabelingContinuousTest`. | implemented | medium | `Verify complete path replay against an independent rational oracle on multi-edge paths whose score-boundary preimages are non-integral.` |
| Definition 4: budget-feasible path/domain (p4) | Retain exactly the departure domain on which `A_P(t)-t <= B`, including correct boundary ownership. | `TimeProfile.domainWhereTravelTimeAtMost`, `CanonicalPathProfileBuilder`; `BudgetRestrictionContinuousTest`, `TimeProfileExactTest`. | implemented | medium | `Add rational-oracle cases where a budget root coincides with a score boundary, a function boundary, and a singleton endpoint.` |
| Definition 5: fixed-departure query (p4) | At one departure time, return the maximum-score feasible simple path, with deterministic ties, or bottom. | `PointPCMaxRunner`, `PACE.bestPointResult`, `ExactPathValidator`; public API oracle tests. Public query times are integer-valued. | partial | medium | `Decide whether fixed departures are intentionally integer-grid only; either expose a continuous-time fixed-departure API or document the narrower implementation contract and paper impact.` |
| Definition 6: interval-best query (p4) | Return one globally best `(departure,path)` over the continuous interval, or bottom. | `IntervalBestAlgorithm` derives a profile with `ExhaustiveProfileAlgorithm` but selects with `EnvelopeProfile.bestResult(..., query::isOnGrid)`. | partial | high | `Implement continuous interval-best selection over all profile cells and score/travel boundaries, or rename and document the existing grid-restricted baseline; validate it independently.` |
| Definition 7: constrained preference path profile (p4-5) | Cover the full interval with path or bottom cells, be pointwise optimal under the stated tie order, and maximally merge adjacent equal assignments. | `EnvelopeProfile`, `EnvelopeExtractor`, `ProfileCellPartition`; the independent `TinyContinuousEnvelopeOracle` compares complete normalized cells, bottom coverage, and endpoint ownership on deterministic and seeded cases. | implemented | medium | `Keep the complete rational-oracle comparison as a required regression gate and expand its seeded graph corpus before changing envelope ranking.` |

## Algorithms 1-6

| Paper requirement | Expected behavior | Evidence found in repository | Status | Risk | Exact TODO prompt |
|---|---|---|---|---|---|
| Algorithm 1: top-level PACE (p8) | Check `T_q=[t_s,t_e+B]`; force `L=K=infinity` for X; build anchors and query memo; generate, filter, and extract the root envelope. | `PACE.run`, `PaceFrontierGenerator.execute`, `AnchorIndex.create`, `EnvelopeExtractor.extract`; horizon CLI tests. PACE-X still accepts caller `theta`, while experiment records claim `EXACT_GLOBAL` from policy alone. | partial | critical | `Introduce separate completion, execution-policy, and exactness-scope metadata; never globally certify PACE-X solely because policy X was requested.` |
| Algorithm 2: `GenerateFrontier` (p10) | Normalize memo domain; generate connector baseline; handle `ell=0`; retain relevant anchors; enumerate all budget splits; recurse, stitch, compress, and memoize. | `PaceFrontierGenerator.generate/computeFrontier`; `MemoKey`; `PaceCoreGenerationContractTest`, `PaceMemoKeyContractTest`. | implemented | medium | `Add branch-complete oracle tests for ell=0, every split, disjoint domains, cache hits, and a path requiring exactly theta anchors.` |
| Algorithm 3: `ConnectorProfiles` (p10-11) | Identity for `u=v`; enumerate simple paths in `G\\A`; lower-bound order/pruning; exact profiles; exhaustive X; first K nonempty bounded connectors. | `ConnectorProfiles.generate/enumerateExhaustive/enumerateBounded`; `PaceCoreGenerationContractTest`. | implemented | medium | `Compare connector arc-ID sequences and domains with an independent exhaustive connector enumerator on parallel-arc and empty-domain fixtures.` |
| Algorithm 4: `TemporalStitch` (p11-13) | Reject self-loop/non-simple concatenations; compute `D0`, `D1`, `D2`, budget domain, and all shifted arrival/score profiles exactly. | `TemporalStitch.stitch`, `PaceFrontierGenerator.imageDomainAfterAnchor`; `TemporalStitchContractTest`, `TemporalStitchContinuousTest`, `PaceP0SynchronizationTest`. | implemented | medium | `Add end-to-end oracle cases for self-loop anchors, repeated vertices, zero-score anchor traversal, flat preimages, and every D0/D1/D2 boundary.` |
| Algorithm 5: `CompressFrontier` (p14-16) | Remove exact duplicates; apply only extension-safe dominance in X; in B retain champion, earliest, least-restrictive, then deterministic fill up to K per cell; merge compatible fragments. | `FrontierCompressor`, `SafeProfileDominance`; `FrontierCompressorTest`. The rational oracle found and now guards the fixed endpoint-only retention defect and compares full PACE-X envelopes. | implemented | medium | `Add a larger seeded compressed-versus-uncompressed frontier corpus before changing dominance or cell retention.` |
| Algorithm 6: `ExtractEnvelope` (p16-17) | Refine at validity, score, ordering, and travel-tie boundaries; choose deterministic winner; emit bottom; maximally merge. | `ProfileCellPartition`, `EnvelopeExtractor`; envelope tests plus the independent rational oracle verify complete cells, coincident boundaries, singleton ownership, bottom coverage, and maximality. | implemented | medium | `Keep exact cell and endpoint-ownership comparison enabled for every future extraction change.` |

## Lemmas, Theorems, and Policy Contract

| Paper requirement | Expected behavior | Evidence found in repository | Status | Risk | Exact TODO prompt |
|---|---|---|---|---|---|
| Lemma 1: candidate soundness (p17-18) | Every retained candidate realizes a simple path with correct profiles, feasible domain, and anchor count. | Canonical replay in `PaceFrontierGenerator.canonicalizeGeneratedPaths`; `ExactPathValidator`; generation contract tests. | partial | high | `Validate every generated candidate against an independent path replay across its complete domain, including boundary singleton atoms.` |
| Lemma 2: temporal composition (p18) | Stitched arrival, travel, and score profiles equal actual path traversal. | `TemporalStitch`; focused continuous stitch tests. | partial | medium | `Differential-test temporal composition with rational affine functions and score pullbacks without using production profile composition in the oracle.` |
| Lemma 3: exact stitched domain (p18) | Stitched domain is exactly the valid and budget-feasible root domain. | D0/D1/D2/budget restrictions in `TemporalStitch`; stitch tests. | partial | high | `Add independent domain equality tests for holes, flat preimages, open/closed endpoints, and coincident budget/function roots.` |
| Lemma 4: profile-form preservation (p18-19) | Stitching preserves nondecreasing PWL arrival and PWC integer score form. | `TimeProfile`, `ScoreProfile`, `TemporalStitch`; exact profile tests. | implemented | medium | `Add generated rational FIFO fixtures and assert both functional form and independent evaluations after multi-edge composition.` |
| Lemma 5: extension-safe dominance (p19) | Prune only under identical arrivals, no-worse score, compatible signature, and tie-key conditions. | `SafeProfileDominance`, `FrontierCompressor`; `FrontierCompressorTest`. | partial | high | `Exhaustively extend dominated and retained fragments on tiny suffix graphs and prove pruning never removes the unique globally winning continuation.` |
| Theorem 1: exact retained-frontier envelope (p19-20) | Envelope extraction is exact for every time over the retained root frontier, including bottom. | `EnvelopeExtractor`; envelope tests and `TinyContinuousEnvelopeOracle` compare complete normalized cells rather than sampled departures. | implemented | medium | `Expand the candidate-frontier oracle corpus while retaining complete-cell equality as the acceptance rule.` |
| Theorem 2: global exactness of PACE-X (p20-21) | All seven conditions must hold: complete anchors, sufficient theta, exhaustive exact connectors, safe anchor filtering, only safe pruning, no candidate-removing limit/filter, and exact temporal boundaries. | X unbounds L/K and the guard aborts. Schema-v2 results now separate policy and scope; current PACE-X is `RETAINED_FRONTIER`, never `GLOBAL_CERTIFIED`. Theta certification and rational temporal arithmetic remain unavailable. | partial | critical | `Implement rational internal profile arithmetic and a machine-readable seven-condition certificate; permit GLOBAL_CERTIFIED only when every condition, including conservative theta sufficiency, is recorded true.` |
| Proposition 1: PACE-B retained-frontier exactness (p21) | B is exact only over its retained frontier and is not generally globally exact. | B applies finite L/K. `ExactnessScope`, CLI output, benchmark schema v2, and adapter tests now serialize `RETAINED_FRONTIER` for successful PACE-B and downgrade failures to `NOT_CERTIFIED`. | implemented | medium | `Keep a schema/CLI regression that rejects any future global PACE-B label and independently validate Theorem-1 retained-frontier behavior.` |
| Exact rational memo/domain representation (p9) | Normalized domains used as memo keys should use exact integer/rational endpoints, not raw floating point. | `Domain` now retains 12 decimal places internally while query budgets retain a separate 9-decimal output unit. Exact roots `80/11`, `228/77`, and `149152/23509` pass the fraction oracle, but production storage remains normalized `double`. | partial | critical | `Introduce rational production endpoint/affine types before enabling GLOBAL_CERTIFIED; retain the current fraction-oracle and 10^-9 output conversion gates.` |

## Sections 6.1-6.6

| Paper requirement | Expected behavior | Evidence found in repository | Status | Risk | Exact TODO prompt |
|---|---|---|---|---|---|
| Section 6.1: experimental setup | Reproducibly state language/compiler/flags, hardware, OS, repetition/query policy, and measured outputs. | Java/Maven build and experiment framework exist. The draft retains placeholders for language, compiler, flags, hardware, OS, query count, and seed. | missing | critical | `Capture build/runtime/hardware provenance in result records and generate the paper setup table from it; never fill placeholders manually.` |
| Section 6.2: datasets | Evaluate the frozen NY, FLA, CAL, and USA scope; OL is removed from the experiment and must not be claimed. | Four-dataset configs and a checksum/count/support preflight exist. The current assets still fail the extended-support requirement. | partial | high | `Regenerate and checksum-validate NY/FLA/CAL/USA with corrected time units and explicit extended support; keep OL absent from matrices, counts, and claims.` |
| Section 6.3: synthetic generation | Support `[0,1440]`, stated rush windows/multipliers, 20% score edges, scores 1-15, stable seed, unique arc IDs, and reproducible generator details. | Manifests record seed 42 and the stated parameters, but omit the conversion from DIMACS's arbitrary transit-weight unit to minutes. A real NY dry run rejects all 20,000 candidates at the lower-bound horizon preflight. Generator source/protocol is absent. | partial | critical | `Choose and record an explicit transit-weight-to-minute normalization, version the generator and provenance, regenerate all payloads, and rerun checksum plus query-balance validation.` |
| Section 6.4: query workload | 100 base pairs per dataset; lengths 120/180/240/300/360 centered at 510 and 1110; rho 0.10-0.50; default rho .30/theta 2; threads 1-32; `B=(1+rho)*continuous T_min`. | Current query generation creates a separate quartile-balanced four-regime study: 96 main queries, starts 420/720/1020/60, lengths 30/60/120/240/360, and main budget `1.25*max(T_fast)`. | missing | critical | `Create a distinct paper-workload configuration and generator path for exactly 100 base pairs and the literal centered-window/min-fastest budget protocol; do not overwrite the separate deterministic quartile study.` |
| Section 6.4 horizon safety | Every derived query must satisfy `interval_end+B <= support_end`; never wrap or extrapolate. | Core rejects uncovered horizons. Current query generator rejects invalid variants. A 360-minute evening window ends at 1290, so budgets above 150 violate `[0,1440]`. | partial | critical | `Add paper-workload preflight that reports violations by dataset/window/rho before generation; resolve by explicit manuscript/data-support decision, never query-time wrapping or extrapolation.` |
| Section 6.5: baselines and ablations | Repeated point query, interval-best, profile labeling, NoComp, NoMemo, Seq, and theta 0-3. | Registry/adapters/configs exist for these algorithms and ablations. Interval-best is grid-restricted; profile-labeling and exactness labels need independent validation. | partial | high | `Add a matrix contract test for every required baseline/ablation and independently validate interval-best and profile-labeling semantics before publication use.` |
| Section 6.6: metrics | Runtime, peak memory, profile size/compression, average/best score, generated/stitched/retained candidates, frontier compression, reference quality gap, and T1/Tp speedup. | Runtime/profile/quality scaffolding exists. `PaceBench` writes `peak_rss` and frontier peaks as null; average profile score, paper compression ratios, and real speedup are absent. | partial | critical | `Instrument every Section 6.6 metric with units and zero-case rules, schema-test non-null successful records, and independently verify summary calculations.` |
| Deterministic parallel execution (p22 and Section 6.4) | Requested worker counts perform real deterministic work and support T1/Tp measurement. | `PaceOptions.threadCount` is recorded, but PACE recursion/connectors do not use an executor; `IPCMaxParallelExecutor` is unused by PACE. | missing | critical | `Implement query-scoped deterministic parallel PACE with canonical merge order and concurrency tests, or remove parallel execution and speedup claims until it exists.` |

## Reproducibility Ambiguities

The draft does not specify the minimum-distance threshold, PRNG identity, sampling replacement policy, pair ordering, congestion peak positions, score subinterval boundaries, rounding rules, repeated-point resolution, warmup/repetition protocol, memory measurement method, failure/timeout policy, or the zero-reference quality-gap rule. It also asks how score density affects behavior but supplies only the fixed 20% density.

These are paper decisions, not values the implementation should invent.
