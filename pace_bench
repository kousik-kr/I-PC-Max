#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
[[ -f "$ROOT/target/pace-bench.jar" ]] || mvn -q -f "$ROOT/pom.xml" -DskipTests package
exec java -jar "$ROOT/target/pace-bench.jar" "$@"
