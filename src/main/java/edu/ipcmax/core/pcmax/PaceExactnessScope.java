package edu.ipcmax.core.pcmax;

/** Exactness certificate carried by a core PACE generation result. */
public enum PaceExactnessScope {
    GLOBAL_CERTIFIED,
    RETAINED_FRONTIER,
    NOT_CERTIFIED
}
