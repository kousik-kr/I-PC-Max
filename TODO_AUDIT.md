# PACE Implementation TODO Audit

This audit now uses the literal numbering and requirements from the supplied 28-page draft. The complete evidence matrix is [docs/audit/PACE_REQUIREMENTS_MATRIX.md](docs/audit/PACE_REQUIREMENTS_MATRIX.md), and corrected conflicts with the prior GPT strategy are in [docs/audit/CONTRADICTION_LOG.md](docs/audit/CONTRADICTION_LOG.md).

Baseline: commit `19bb54b36cf3344244ad62819b9510c5df53f15b`, dirty `main` worktree, Java 21.0.11, Maven 3.9.16, and 134/134 tests passing before audit changes on 2026-07-21. Current verification: 152/152 tests passing.

## Highest-Priority TODOs

| Paper requirement | Expected behavior | Evidence found in repo: file/function/test | Status | Risk level | Exact TODO prompt to fix or verify it |
|---|---|---|---|---|---|
| Theorem 2 global PACE-X exactness | Global certification only when all seven conditions hold, including sufficient theta and exact temporal boundaries. | Schema-v2 status now separates policy/scope; successful PACE-X is `RETAINED_FRONTIER`, failures are `NOT_CERTIFIED`, and no adapter is currently global-certified. Theta and rational-arithmetic certificates remain absent. | partial | critical | `After rational arithmetic passes the oracle, add a seven-condition Theorem-2 certificate and permit GLOBAL_CERTIFIED only when every condition is recorded true.` |
| Independent complete-envelope validation | All simple paths and all exact temporal cells, endpoint ownership, and bottom cells must agree with PACE-X. | The independent fraction oracle covers ten deterministic/seeded methods; all pass, including the multi-edge budget root, endpoint-only feasibility, and 12 fixed-seed FIFO DAGs. | implemented | high | `Expand the rational differential corpus before changing profile/domain arithmetic, and keep complete cell/ownership comparison as a required CI gate.` |
| Section 4.2 numerical representation | Normalized memo domains and temporal roots use exact integer/rational representation rather than raw floating point. | Production uses 12-decimal guarded normalization internally and a separate 9-decimal query-budget output unit. This passes the fraction oracle but is not arbitrary-rational storage. | partial | critical | `Replace normalized-double endpoint and affine storage with rational production types before enabling GLOBAL_CERTIFIED; preserve the existing 10^-9 query-budget serialization contract.` |
| Generated-dataset temporal units | DIMACS transit weights must be converted to the same minute unit as `[0,1440]`, with the conversion recorded in reproducible metadata. | Real NY smoke output contains edge time `2008.0`; the official DIMACS source defines an arbitrary transit-time unit. NY dry-run rejects all 20,000 candidates by the new lower-bound horizon preflight. No conversion field or generator source is present. | missing | critical | `Choose and document the DIMACS-weight-to-minute normalization in the paper/data-generation protocol, regenerate all dataset functions and manifests with unit metadata, then rerun deterministic query generation; do not convert implicitly in GeneratedGraphLoader.` |
| Section 6.4 paper workload | 100 pairs per dataset, two rush-centered window families, lengths 120-360, rho 10-50%, continuous minimum-fastest budget, theta 0-3, threads 1-32. | `query_generation.yaml` implements a different four-regime, quartile-balanced 96-main-query design and a `1.25*max(T_fast)` main budget. | missing | critical | `Add a separate paper-workload configuration and generator without replacing the deterministic quartile study; use the existing continuous fastest-profile facility for T_min.` |
| Algorithm 1 function horizon | Reject every query for which `interval_end+B` exceeds support; no wrap, clip, or extrapolation. | Core horizon rejection exists. `QueryBudgetBuilder` now rejects before profile construction when even `1+rho` times the admissible lower bound crosses support. Paper evening windows can still be intrinsically invalid. | partial | critical | `Generate a preflight violation report by dataset, window, and rho, then record the required workload/data-support manuscript decision.` |
| Deterministic parallel PACE | Thread counts 1-32 perform real deterministic PACE work and permit meaningful T1/Tp speedup. | `PaceOptions.threadCount` is metadata only in PACE; `IPCMaxParallelExecutor` is unused by the PACE implementation. | missing | critical | `Implement query-scoped deterministic parallel candidate work with canonical merging and instrumented concurrency tests, or remove the unsupported parallel claims.` |
| Sections 6.1 and 6.6 reproducibility/metrics | Capture setup provenance and every runtime, memory, profile, compression, quality, and speedup metric. | `PaceBench` has result scaffolding but writes multiple memory/frontier fields as null and omits paper aggregates; the draft retains setup placeholders. | partial | critical | `Instrument and schema-test every Section 6.6 metric, capture environment provenance, and generate paper setup/results from recorded data.` |
| Definition 6 interval-best baseline | Select one globally best route-departure pair over the continuous interval. | `IntervalBestAlgorithm` builds an exhaustive profile but selects only departures satisfying `QuerySpec.isOnGrid`. | partial | high | `Implement continuous interval-best selection or explicitly rename the current grid baseline and revise the paper comparison.` |
| Sections 6.2-6.3 datasets | Validate OL, NY, FLA, CAL, USA and reproduce synthetic generation. | NY/FLA/CAL/USA payloads and manifests exist in this worktree; OL and versioned generator provenance are absent. | partial | high | `Add OL plus an all-dataset checksum/count/support preflight, and version the exact synthetic generator protocol without fabricating missing data.` |
| Algorithms 4-6 and Theorem 1 | Stitching, compression, and extraction preserve exact domains and choose the correct retained-frontier path for every time. | Strong focused unit tests exist, but no independent rational full-envelope/property harness exists at baseline. | partial | high | `Use the independent oracle to verify D0/D1/D2, flat preimages, safe dominance, coincident score/travel roots, bottom coverage, and maximal merging.` |

## Paper Ambiguities

The draft must resolve the minimum-distance threshold, PRNG and pair-sampling policy, generator peak/subinterval details, baseline resolution, repetitions/warmup, memory method, failure handling, quality-gap zero case, the missing score-density sweep, and the remaining `<...>` setup placeholders. These are blockers for a paper-reproduction claim, not license to invent values.
